package dev.senj.submarine.model;

/**
 * Defines the approved types of commands the ROV can execute.
 */
public enum CommandType {
    SET_THRUST_FORWARD,
    SET_THRUST_VERTICAL,
    SET_YAW,
    ADJUST_CAMERA_PITCH
}
